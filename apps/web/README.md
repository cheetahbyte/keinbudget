## Web App

Install dependencies from the repo root:

```sh
pnpm install
```

Run the frontend in development:

```sh
pnpm --filter @keinbudget/web dev
```

Build the production server:

```sh
pnpm --filter @keinbudget/web build
```

Run tests:

```sh
pnpm --filter @keinbudget/web test
```
